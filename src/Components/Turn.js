import React from 'react';
import { Grid, Row } from 'react-bootstrap';

import './Turn.css';

const Turn = () => {
	return (
		<Grid fluid={true}>
			<Row>Turn</Row>
		</Grid>
	)
};

export default Turn;
